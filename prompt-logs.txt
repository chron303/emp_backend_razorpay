1.read folder.scheme.txt and make the schema accordingly
2.Implement the onboarding module for the reimbursement management system.
Architecture:

src/
├── routes/
├── controllers/
├── services/
├── validators/
├── middlewares/
├── utils/

Authentication:
- Cookie-based JWT authentication.
- Login must set an auth cookie.
- Protected routes will later read the auth cookie.
- JWT should contain userId and role.

Role Rules:
- Every newly registered account starts with EMP role.
- Users cannot choose their role during registration.
- Only emails ending with @org.com are allowed.

Endpoints:

POST /rest/onboardings/register

Request Body:
{
  "name": "John Doe",
  "email": "john@org.com",
  "password": "password123"
}

Rules:
- Validate required fields.
- Validate org.com domain.
- Email must be unique.
- Hash password before storing.
- Assign EMP role automatically.
- Do NOT log the user in.

POST /rest/onboardings/login

Request Body:
{
  "email": "john@org.com",
  "password": "password123"
}

Rules:
- Validate required fields.
- Validate org.com domain.
- Verify credentials.
- Generate JWT.
- Set auth cookie.
- Return success response.

POST /rest/onboardings/logout

Rules:
- Clear auth cookie.
- Return success response.

Implementation Requirements:
- Keep controllers thin.
- Put business logic inside auth.service.js.
- Put validation inside auth.validator.js.
- Use existing ROLES enum.
- Use standardized response helper.
- Follow SOLID principles.

First provide ONLY:
1. auth.validator.js responsibilities
2. auth.service.js responsibilities
3. onboarding.controller.js responsibilities
4. onboarding.routes.js responsibilities

Do not generate code yet.
3.Implement the Code
4.generate drizzle/schema.js 
build 3 tabls with following schema designed in db_table_schema.txt
use postgreSQL drizzle schema definations 
use pgTable 
export all tables use proper foreign keys 
use timestamps at createdAT and udpatedAT
5.Generate drizzle/relations.js for the finalized schema.
Requirements:
Tables:
users
employee_manager
reimbursements
Define all Drizzle relations.

Relationships:

One employee_manager record belongs to one employee user.
One employee_manager record belongs to one manager user.
One employee can create many reimbursements.
One reimbursement belongs to one employee.

Return only drizzle/relations.js code.
6.generate migrate.js 
use DATABASE_URL from .env
execute migrations from drizzle/migration
log the messages
 return the code


7.generate seed.js 
database has 3 tables 
users 
employee_manager
reimbursements 

seed only the cfo account 

cfo details 
email:cfo@org.com
password:CFO#ORG@April2026


rules:
import users table from schema.js 
import roles from src/enums/roles.enums.js
use bcrypt hashing 
check if cfo already exists by email 
do not create duplicate 
if exists log a messaeg and exit succesfully 
if dont then create cfo account 
log success message after creation
handle the errors


dont seed EMP,RM,APE users 
dont seed reimbursements
dont seed employee-manager mappings 

8. complete and generate drizzle.config.json run npm run db:generate